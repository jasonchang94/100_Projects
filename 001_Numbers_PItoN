##Find PI to the Nth Digit - Enter a number and have the program generate PI up to that many decimal places. Keep a limit to how far the program will go.
__author__ = 'JasonChang'
from math import pi

def PItoN():
    number = int(input("What is your precision desired?"))

    while number>50:
        print("Try again too large precision")
        number = input("What is your precision desired?")
    else:
        precision = "%.*f" %(number,pi)
        return(precision)
print(PItoN())
